{
  "name": "recur",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/migrate.ts && bun run src/index.ts",
    "migrate": "bun run src/migrate.ts",
    "seed": "bun run src/seed.ts"
  },
  "module": "src/index.ts",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}